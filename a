1
#include <stdio.h> #include <string.h> int main() { char username[15]; char password[12]; printf("Enter your username:\n"); scanf("%s", 
username); printf("Enter your password:\n"); scanf("%s", password); if(strcmp(username,"admin")==0) { if(strcmp(password,"admin")==0) { 
printf("\n Welcome. Login Success!"); } else { printf("\n Password is Incorrect"); } } else { printf("\n User dosen't exist"); return 0; }


2
#include <stdio.h>#include <string.h>int main() { int i,x;char str[100]; printf("\n enter a string:");fgets(str,sizeof(str),stdin); printf("\n choose the 
following options:\n");printf("1=Encrypt the string.\n"); printf("2=Decrypt the string.\n");scanf("%d",&x); switch(x) {case 1:for(i=0;i<100 && 
str[i] !='\0';i++)str[i]=str[i]+3;printf("\n Encrypted string:%s\n",str);break;case 2:for(i=0;i<100 && str[i] !='\0';i++)str[i]=str[i]-3;printf("\n 
Decrypted string:%s\n",str);break;default:printf("\nError\n");}return 0;}
3
#include <stdio.h>#include <string.h> char dc[26]="XDGSZANYOBTMJCEVFHKWPLQURI"; char ec[26]="abcdefghijklmnopqrstuvwxyz"; void 
e(char*);void d(char*); int main(){ char p[100]; printf("\n\n Enter plain text:");scanf("%s",p); printf("\n After Encryption:\n");e(p);printf("\n 
After Decryption:\n"); d(p); printf("%s",p);return 0;}void e(char *p){ int i=0; while(*(p + i) != '\0'){ *(p + i) = dc[*(p + i) - 97]; i++;}}void d(char 
*p){int i=0; while(*(p+i) !='\0') {int i=0;for(i=0;i<26;i++){if(dc[i]==*(p+i))break;} *(p+i)=ec[i];i++;}}


4
#include <stdio.h> #include <stdlib.h>#include <math.h> #include <string.h>long int p, q, n, t, flag, e[100], d[100], temp[100],j, m[100], en[200], 
i;char msg[100];int prime(long int);void ce();long int cd (long int);void encrypt();void decrypt();int main(){printf("\nENTER FIRST PRIME 
NUMBER\n");scanf("%ld", &p);flag = prime(p);if (flag == 0) {printf("\nWRONG INPUT \n");exit(1);}printf("\nENTER ANOTHER PRIME NUMBER 
\n");scanf("%ld", &q);flag = prime(q);if (flag == 0 || p == q) {printf("\nWRONG INPUT\n");exit(1);}printf("\nENTER 
MESSAGE\n");fflush(stdin);scanf("%s", msg);for (i = 0; msg[i] !='\0'; i++){m[i] = msg[i];}n=p*q;t = (p - 1) * (q - 1);ce();printf("\nPOSSIBLE VALUES 
OF e AND d ARE\n");for (i = 0; i< j-1; i++){printf("\n%ld \t%ld", e[i], d[i]);}encrypt();decrypt();return 0;}int prime(long int pr)int i;j= sqrt(pr);for (i = 
2; i<= j; i++){if (pr % i == 0)return 0;}return 1;void ce(){int k;k= 0;for (i = 2; i<t; i++) {if (t% i== 0)continue;flag = prime(i);if (flag == 1 &&i != p &&i 
!= q){e[k] = i;flag = cd(e[k]);if (flag > 0){d[k] = flag;k++;}if (k == 99)break;}}j=k; // Setting jfor the main loop limit}long int cd (long int x){long int k = 
1;while (1) {k= k+t;if (k%x==0)return (k / x);}}void encrypt(){long int pt, ct, key = e[0], k, len;i = 0;len = strlen(msg);while (i!= len){pt=m[i];pt = pt -
96;k = 1;for (int jj = 0; jj< key; jj++){k=k* pt;k= k% n;temp[i] = k;ct = k + 96;en[i] = ct;i++;}en[i] = -1;printf("\nTHE ENCRYPTED MESSAGE IS\n");for 
(i = 0; en[i] != -1; i++){printf("%c", (char)en[i]);}}void decrypt(){long int pt, ct, key = d[0], k;i = 0;while (en[i]!=-1){ct = temp[i];k = 1;for (int jj = 0; 
jj< key; jj++){k = k* ct;k= k% n;}pt = k + 96;m[i] = pt;i++;}m[i] = -1;printf("\nTHE DECRYPTED MESSAGE IS\n");for (i = 0; m[i] != -1; i++){printf("%c", 
(char)m[i]);}printf("\n");}


5
clc; g=7; p=23; printf('\n The results are as follows:\n\n"); x=3; y=6; R1=modulo(g^x,p); R2=modulo(g^y,p); printf("1)Alice choose x=%d & 
calculated R1=%d\n\n2) Bob chooses y=%d & calculates R2=%d\n\n3) Alice sends the number %d to Bob \n\n4) Bob sends the number %d to 
Alice\n\n", x, R1, y, R2, R1, R2);K_Alice=modulo((R2)^x,p);K_Bob=modulo((R1)^y,p);K_Final=modulo(g(x*y),p);printf('5)Alice caluculates the 
symmetric key K=%d\n\n6) Bob calculates the symmetric key k=%d\n\n7) K_Final=%d\n\n', K_Alice, K_Bob, K_Final);
